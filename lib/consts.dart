import 'package:flutter/material.dart';
class ProjectImages{
  static const String yesilDunyaImages = "assets/images/yesildünya.png";
}
class ProjectHeight{
  static const double containerSmallHeigth = 50;

}
class ProjectWidth{
  static const double containerSmallWidth = 50;
  static const double containerSmallWidth3x = 150;
}

class ProjectPadding{
  static const EdgeInsets projectPadding = EdgeInsets.all(20);
  static const EdgeInsets projectNormalTopPadding = EdgeInsets.only(top: 30);
  static const EdgeInsets ProjectLargeTopPadding = EdgeInsets.only(top: 60);
}

class ProjectRadius{
  static const BorderRadius normalBorderRadius = BorderRadius.all(Radius.circular(10.0));
}